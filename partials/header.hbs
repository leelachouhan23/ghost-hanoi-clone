<header class="site-header">
  <nav class="site-nav">
    <a class="site-logo" href="{{@site.url}}">
      {{#if @site.logo}}
        <img src="{{@site.logo}}" alt="{{@site.title}}">
      {{else}}
        {{@site.title}}
      {{/if}}
    </a>

    {{navigation}}
  </nav>
</header>