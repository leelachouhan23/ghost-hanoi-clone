<article class="post-card {{post_class}}">
    <!-- Featured Image (Only rendered if featured image exists in Ghost Admin) -->
    {{#if feature_image}}
        <div class="post-card-media">
            <a href="{{url}}" aria-label="{{title}}">
                <img class="post-card-image"
                     srcset="{{img_url feature_image size="s"}} 300w,
                             {{img_url feature_image size="m"}} 600w,
                             {{img_url feature_image size="l"}} 1000w"
                     sizes="(max-width: 768px) 100vw, (max-width: 1024px) 50vw, 33vw"
                     src="{{img_url feature_image size="m"}}"
                     alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
                     loading="lazy"
                >
            </a>
        </div>
    {{/if}}

    <!-- Content -->
    <div class="post-card-content">
        <!-- Tag Badge -->
        {{#if primary_tag}}
            <a href="{{primary_tag.url}}" class="post-card-tag">
                {{primary_tag.name}}
            </a>
        {{/if}}

        <!-- Title -->
        <h2 class="post-card-title">
            <a href="{{url}}">{{title}}</a>
        </h2>

        <!-- Excerpt -->
        {{#if excerpt}}
            <p class="post-card-excerpt">
                {{excerpt words="22"}}
            </p>
        {{/if}}

        <!-- Post Meta -->
        <div class="post-card-meta">
            {{#if primary_author}}
                {{#if primary_author.profile_image}}
                    <img class="author-avatar" src="{{img_url primary_author.profile_image size="xs"}}" alt="{{primary_author.name}}">
                {{/if}}
                <span class="author-name">{{primary_author.name}}</span>
                <span class="meta-dot">•</span>
            {{/if}}
            <time datetime="{{date format="YYYY-MM-DD"}}">{{date format="MMM DD, YYYY"}}</time>
        </div>
    </div>
</article>
